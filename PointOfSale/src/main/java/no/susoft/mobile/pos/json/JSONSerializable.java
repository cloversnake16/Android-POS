package no.susoft.mobile.pos.json;

/**
 * Indicate that this class will be serialized into a JSON file so that Proguard preserves (skips)
 * the class during obfuscation.
 *
 * @author Yesod
 */
public interface JSONSerializable {

}